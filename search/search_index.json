{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"ONNX Runtime Python Developer Guide","text":"<p>This guide helps Python developers integrate ONNX Runtime into machine learning inference applications.</p> <p>It covers ONNX Runtime concepts, inference workflows and Python API usage.</p>"},{"location":"#overview","title":"Overview","text":"<p>Learn what ONNX Runtime is, how it works with ONNX models, and how Python applications use it for inference.</p> <ul> <li>ONNX Runtime Overview</li> </ul>"},{"location":"#get-started","title":"Get Started","text":"<p>Set up your environment and run your first inference example.</p> <ul> <li>Installing ONNX Runtime</li> <li>Running Your First Inference</li> </ul>"},{"location":"#core-workflow","title":"Core Workflow","text":"<p>Learn the five steps for running inference with the ONNX Runtime Python API.</p> <ul> <li>Five Steps for Any Model</li> </ul>"},{"location":"#python-api","title":"Python API","text":"<p>Learn how to use the ONNX Runtime Python API to create inference applications.</p> <ul> <li>InferenceSession API Reference</li> </ul>"},{"location":"core-flow/five-steps/","title":"Core Workflow: Five Steps for Any Model","text":"<p>Once you have an ONNX model, the basic inference workflow in the ONNX Runtime Python API can be described in five steps.</p> <pre><code>flowchart TD\n    A[Load the Model] --&gt; B[Inspect Inputs and Outputs]\n    B --&gt; C[Prepare Input Data]\n    C --&gt; D[Run Inference]\n    D --&gt; E[Process Output]</code></pre> <p>This page uses a generic model to explain the workflow. The exact input preprocessing and output processing depend on the model.</p>"},{"location":"core-flow/five-steps/#step-1-load-the-model","title":"Step 1: Load the Model","text":"<p>Create an <code>InferenceSession</code> to load the ONNX model:</p> <pre><code>import onnxruntime as ort\n\nsession = ort.InferenceSession(\"your-model.onnx\")\n</code></pre> <p>The session loads the model and prepares the runtime environment for inference.</p>"},{"location":"core-flow/five-steps/#step-2-inspect-inputs-and-outputs","title":"Step 2: Inspect Inputs and Outputs","text":"<p>Every model has its own input and output names, shapes, and types. Inspect the model interface before preparing input data:</p> <pre><code>for inp in session.get_inputs():\n    print(f\"Input: name={inp.name}, shape={inp.shape}, type={inp.type}\")\n\nfor out in session.get_outputs():\n    print(f\"Output: name={out.name}, shape={out.shape}, type={out.type}\")\n</code></pre> <p>For details on these methods, see <code>get_inputs()</code> and <code>get_outputs()</code>.</p> <p>Use the returned metadata to prepare input data that matches the model requirements.</p>"},{"location":"core-flow/five-steps/#step-3-prepare-input-data","title":"Step 3: Prepare Input Data","text":"<p>Create input data that matches the model's expected shape and type:</p> <pre><code>import numpy as np\n\n# Replace with data that matches the model interface\ninput_data = np.array([...], dtype=np.float32)\n</code></pre> <p>The exact preprocessing depends on the model. For example:</p> <ul> <li>Image models may require resizing, normalization, and channel reordering.</li> <li>Text models may require tokenization and tensor conversion.</li> <li>Tabular models may require feature scaling.</li> </ul> <p>Always consult the model's documentation for model-specific preprocessing requirements.</p>"},{"location":"core-flow/five-steps/#step-4-run-inference","title":"Step 4: Run Inference","text":"<p>Pass the input data to <code>session.run()</code>:</p> <pre><code>input_name = session.get_inputs()[0].name\noutputs = session.run(None, {input_name: input_data})\n</code></pre> <ul> <li>The first argument, <code>None</code>, requests all model outputs.</li> <li>The second argument maps the input name to the input tensor.</li> </ul>"},{"location":"core-flow/five-steps/#step-5-process-output","title":"Step 5: Process Output","text":"<p>Model outputs are numerical values. Convert them into meaningful results according to the model's output definition.</p> <p>For example, for a classification model:</p> <pre><code>result = np.argmax(outputs[0])\nprint(result)\n</code></pre> <p>The interpretation of the output depends on the model. Refer to the model's documentation to understand the output format and semantics.</p>"},{"location":"get-started/first-inference/","title":"Running Your First Inference","text":"<p>This section shows how to use the ONNX Runtime Python API to run inference with the <code>mnist-8.onnx</code> model on a handwritten digit image.</p>"},{"location":"get-started/first-inference/#prerequisites","title":"Prerequisites","text":"<p>Before starting, make sure that:</p> <ul> <li>ONNX Runtime is installed. See Installing ONNX Runtime.</li> <li>NumPy and Pillow are installed:</li> </ul> <pre><code>pip install numpy pillow\n</code></pre>"},{"location":"get-started/first-inference/#sample-files","title":"Sample Files","text":"<p>The example uses the following files:</p> File Description <code>quickstart.py</code> Complete inference script <code>mnist-8.onnx</code> ONNX model for handwritten digit recognition <code>digit.png</code> Input image used by the example <p>The sample files are available in the <code>Examples/</code> directory of the project repository.</p> <p>To get started, clone the repository:</p> <pre><code>git clone https://github.com/Emily-doc94/doc-engineering-demo.git\ncd doc-engineering-demo\n</code></pre>"},{"location":"get-started/first-inference/#run-the-example","title":"Run the Example","text":"<p>Run the complete example from the repository root:</p> <pre><code>python examples/quickstart.py\n</code></pre> <p>The script prints the predicted digit for the sample image:</p> <pre><code>Predicted digit: 6\n</code></pre> <p>The output depends on the input image. If you use a different image, the predicted digit may change.</p>"},{"location":"get-started/first-inference/#understand-the-code","title":"Understand the Code","text":""},{"location":"get-started/first-inference/#load-the-model","title":"Load the Model","text":"<pre><code>import onnxruntime as ort\n\nsession = ort.InferenceSession(\"mnist-8.onnx\")\n</code></pre> <p><code>InferenceSession</code> loads the model and prepares the runtime session for inference.</p>"},{"location":"get-started/first-inference/#preprocess-the-input-image","title":"Preprocess the Input Image","text":"<p>The MNIST model expects a <code>[1, 1, 28, 28]</code> float32 tensor representing one grayscale image. Pixel values are normalized to <code>[0.0, 1.0]</code>.</p> <pre><code>from PIL import Image\nimport numpy as np\n\n# Read and convert to grayscale\nimage = Image.open(\"digit.png\").convert(\"L\")\n\n# Resize to 28\u00d728\nimage = image.resize((28, 28))\n\n# Normalize to [0, 1]\nimg_array = np.array(image).astype(np.float32) / 255.0\n\n# Match model input shape\ninput_data = img_array.reshape(1, 1, 28, 28)\n</code></pre>"},{"location":"get-started/first-inference/#run-inference","title":"Run Inference","text":"<pre><code>input_name = session.get_inputs()[0].name\noutputs = session.run(None, {input_name: input_data})\n</code></pre> <p>The script gets the model's input name dynamically and uses it as the key in the input dictionary.</p> <ul> <li>The first argument, <code>None</code>, requests all model outputs.</li> <li>The second argument maps the model input name to the input tensor.</li> </ul>"},{"location":"get-started/first-inference/#get-the-prediction","title":"Get the Prediction","text":"<pre><code>predicted_digit = np.argmax(outputs[0])\nprint(\"Predicted digit:\", predicted_digit)\n</code></pre> <p>For this classification model, <code>np.argmax()</code> returns the index of the highest output score, which corresponds to the predicted digit.</p>"},{"location":"get-started/installation/","title":"Installing ONNX Runtime","text":"<p>This section explains how to install ONNX Runtime for Python applications and verify the installation.</p>"},{"location":"get-started/installation/#prerequisites","title":"Prerequisites","text":"<p>Before installing ONNX Runtime, make sure that:</p> <ul> <li>Python is installed and available in your environment.</li> <li>Your Python version is supported by the ONNX Runtime release you plan to install.</li> <li><code>pip</code> is available for installing Python packages.</li> </ul> <p>For the latest Python version requirements and environment compatibility information, see the ONNX Runtime compatibility documentation.</p>"},{"location":"get-started/installation/#install-onnx-runtime","title":"Install ONNX Runtime","text":"<p>ONNX Runtime provides different Python packages for different execution environments.</p> Package Use case <code>onnxruntime</code> Run inference on the CPU <code>onnxruntime-gpu</code> Run inference with supported GPU execution providers <p>For the examples in this guide, use the CPU package. It provides the simplest setup for learning the ONNX Runtime Python API.</p> <p>Note: Do not install <code>onnxruntime</code> and <code>onnxruntime-gpu</code> in the same Python environment.</p>"},{"location":"get-started/installation/#install-the-cpu-package","title":"Install the CPU Package","text":"<pre><code>pip install onnxruntime\n</code></pre>"},{"location":"get-started/installation/#install-the-gpu-package","title":"Install the GPU Package","text":"<p>If your application requires GPU acceleration, install the GPU package instead:</p> <pre><code>pip install onnxruntime-gpu\n</code></pre> <p>GPU execution requires additional dependencies and a compatible execution provider. For GPU-specific requirements, see the ONNX Runtime installation documentation.</p>"},{"location":"get-started/installation/#verify-the-installation","title":"Verify the Installation","text":"<p>After installing ONNX Runtime, verify that the package can be imported successfully.</p> <pre><code>import onnxruntime as ort\n\nprint(ort.__version__)\n</code></pre> <p>If the installation is successful, the command prints the installed ONNX Runtime version.</p>"},{"location":"overview/onnx-runtime-overview/","title":"ONNX Runtime Overview","text":"<p>This page introduces ONNX and ONNX Runtime, explains how they work together, and shows how a Python application uses ONNX Runtime for inference.</p>"},{"location":"overview/onnx-runtime-overview/#what-is-onnx","title":"What Is ONNX?","text":"<p>ONNX (Open Neural Network Exchange) is an open format for representing machine learning models.</p> <p>It provides a common model representation that allows models developed with different machine learning frameworks to be transferred to different tools and runtimes.</p> <p>For example, a model developed with PyTorch can be exported to the ONNX format and then loaded by ONNX Runtime for inference.</p>"},{"location":"overview/onnx-runtime-overview/#what-is-onnx-runtime","title":"What Is ONNX Runtime?","text":"<p>ONNX Runtime is a cross-platform inference engine for executing machine learning models in the ONNX format.</p> <p>For Python applications, ONNX Runtime provides APIs for:</p> <ul> <li>Loading ONNX models</li> <li>Creating inference sessions</li> <li>Inspecting model inputs and outputs</li> <li>Running inference</li> <li>Configuring execution providers</li> </ul>"},{"location":"overview/onnx-runtime-overview/#onnx-and-onnx-runtime","title":"ONNX and ONNX Runtime","text":"<p>ONNX defines how a machine learning model is represented, while ONNX Runtime provides the software environment for executing the model.</p> <p>A typical workflow is:</p> <pre><code>flowchart LR\n    A[PyTorch / TensorFlow]\n    --&gt;|Export| B[ONNX Model&lt;br/&gt;.onnx file]\n\n    B --&gt; C[ONNX Runtime]\n\n    D[Python Application]\n    --&gt; E[ONNX Runtime Python API]\n    --&gt; C\n\n    C --&gt; F[Execution Provider]\n    --&gt; G[CPU / GPU / Other Hardware]</code></pre> <p>In a typical workflow:</p> <ol> <li>A model is developed or trained using a machine learning framework such as PyTorch or TensorFlow.</li> <li>The model is exported to the ONNX format.</li> <li>A Python application loads the ONNX model through the ONNX Runtime Python API.</li> <li>ONNX Runtime executes the model using a configured execution provider.</li> <li>The execution provider runs the model on the available hardware.</li> </ol> <p>The model creation and training process is outside the scope of this guide. This guide focuses on using ONNX Runtime to run inference from Python applications.</p>"},{"location":"overview/onnx-runtime-overview/#how-python-applications-use-onnx-runtime","title":"How Python Applications Use ONNX Runtime","text":"<p>A Python application interacts with ONNX Runtime through its Python API rather than directly managing hardware execution.</p> <p>The basic relationship is:</p> <pre><code>flowchart LR\n    A[Python Application]\n    --&gt; B[ONNX Runtime Python API]\n    --&gt; C[InferenceSession]\n    --&gt; D[ONNX Runtime]\n    --&gt; E[Execution Provider]\n    --&gt; F[CPU / GPU / Other Hardware]</code></pre> <p>Each component has a different role:</p> Component Role Python Application Provides model input data and processes inference results ONNX Runtime Python API Provides the application-facing interface <code>InferenceSession</code> Loads the model and manages an inference session ONNX Runtime Executes the ONNX model Execution Provider Connects ONNX Runtime to a specific hardware backend CPU / GPU / Other Hardware Executes the model operations <p>For most Python applications, developers work primarily with the Python API and <code>InferenceSession</code>. ONNX Runtime handles the underlying model execution.</p>"},{"location":"python-api/inference-session/","title":"InferenceSession API Reference","text":"<p>InferenceSession API Reference</p> <p>This chapter provides reference information for the <code>InferenceSession</code> class, the primary Python interface for running inference with ONNX Runtime.</p>"},{"location":"python-api/inference-session/#overview","title":"Overview","text":"<p><code>InferenceSession</code> loads an ONNX model into memory and exposes methods for inspecting model metadata and executing inference.</p> <pre><code>import onnxruntime as ort\n\nsession = ort.InferenceSession(\"model.onnx\")\n</code></pre> <p>A session can be reused for multiple <code>run()</code> calls without reloading the model.</p>"},{"location":"python-api/inference-session/#constructor","title":"Constructor","text":"<pre><code>ort.InferenceSession(\n    path_or_bytes,\n    sess_options=None,\n    providers=None,\n)\n</code></pre> Parameter Type Required Description <code>path_or_bytes</code> <code>str</code> or <code>bytes</code> Yes Path to the ONNX model file, or serialized model bytes. <code>sess_options</code> <code>SessionOptions</code> No Session-level configuration such as thread count and graph optimization settings. <code>providers</code> <code>list[str]</code> No Execution providers to use, specified in priority order. If omitted, ONNX Runtime uses the available providers with its default precedence."},{"location":"python-api/inference-session/#methods","title":"Methods","text":""},{"location":"python-api/inference-session/#get_inputs","title":"<code>get_inputs()</code>","text":"<p>Returns metadata for all model inputs.</p> <pre><code>inputs = session.get_inputs()\n</code></pre> <p>Returns: A list of <code>NodeArg</code> objects.</p> Attribute Description <code>name</code> Input node name. Use this as the key in <code>run()</code>. <code>shape</code> Expected tensor dimensions, for example <code>[1, 3, 224, 224]</code>. <code>type</code> Expected data type, such as <code>tensor(float)</code>. <p>Example:</p> <pre><code>for inp in session.get_inputs():\n    print(f\"name={inp.name}, shape={inp.shape}, type={inp.type}\")\n</code></pre>"},{"location":"python-api/inference-session/#get_outputs","title":"<code>get_outputs()</code>","text":"<p>Returns metadata for all model outputs.</p> <pre><code>outputs = session.get_outputs()\n</code></pre> <p>Example:</p> <pre><code>for out in session.get_outputs():\n    print(f\"name={out.name}, shape={out.shape}, type={out.type}\")\n</code></pre>"},{"location":"python-api/inference-session/#run","title":"<code>run()</code>","text":"<p>Executes inference with the given input data.</p> <pre><code>outputs = session.run(\n    output_names=None,\n    input_feed={},\n    run_options=None,\n)\n</code></pre> Parameter Type Required Description <code>output_names</code> <code>list[str]</code> or <code>None</code> No Names of outputs to return. Pass <code>None</code> to return all outputs. <code>input_feed</code> <code>dict</code> Yes Maps input names to input data. Keys must match the model input names. <code>run_options</code> <code>RunOptions</code> No Per-call execution settings. Optional for most use cases. <p>Returns: A list containing the requested model outputs.</p> <p>Example:</p> <pre><code>input_name = session.get_inputs()[0].name\noutputs = session.run(None, {input_name: input_data})\n</code></pre>"},{"location":"python-api/inference-session/#get_providers","title":"<code>get_providers()</code>","text":"<p>Returns the execution providers registered for the session.</p> <pre><code>providers = session.get_providers()\nprint(providers)\n</code></pre> <p>Example output:</p> <pre><code>['CPUExecutionProvider']\n</code></pre> <p>Use this method when you need to inspect which execution providers are available to the session.</p>"}]}